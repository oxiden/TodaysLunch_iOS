# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CommonFramework' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CommonFramework

  target 'CommonFrameworkTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'TodayExtension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TodayExtension
  pod 'Alamofire', '~> 4.0'

end

target 'TodaysLunch' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TodaysLunch

  target 'TodaysLunchTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TodaysLunchUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
